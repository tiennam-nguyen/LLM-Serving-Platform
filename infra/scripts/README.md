# Infrastructure Scripts

This directory contains utility scripts for host system setup, CUDA/driver verification, and lab environment provisioning.

---

## Future Scope

- GPU diagnostic and health checking scripts (`nvidia-smi` wrappers, CUDA version verification).
- Worker node launch and cleanup automation.
