# Monitoring Infrastructure

This directory stores configuration files for telemetry collection, metrics scraping, and visualization dashboards.

---

## Future Scope

- Prometheus scrape configs for gateway and worker adapter metrics.
- Grafana dashboard templates for tracking TTFT percentiles, request rates, token generation throughput, and GPU memory usage.
