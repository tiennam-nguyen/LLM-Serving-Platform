# Benchmark Configurations

This directory stores configuration files (e.g., YAML / JSON) defining workload scenarios, arrival patterns, concurrency settings, and target endpoints for benchmark replay.

---

## Future Contents

- Target endpoint definitions (e.g., local gateway vs. standalone worker).
- Traffic arrival models:
  - Constant arrival rate (requests/sec)
  - Poisson arrival process
  - Burst / step-function loads
- Request length distributions (input prompt tokens, max output tokens).
