# Benchmark Workload Definitions

This directory stores reusable workload specifications and prompt templates used across evaluation runs.

---

## Future Structure

- `synthetic/`: Generated prompt sets with controlled prompt/response token ratios (e.g., short-in/long-out, long-in/short-out).
- `realistic/`: Prompt sequences extracted from domain-specific or open-ended user interactions.
